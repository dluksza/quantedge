[workspace]

[package]
name = "quantedge-strategy-example"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
quantedge-strategy = { path = "../" }

[dev-dependencies]
quantedge-strategy = { path = "../", features = ["test-util"] }
