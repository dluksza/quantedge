[workspace]

[package]
name = "tickglide-strategy-example"
version = "0.1.0"
edition = "2024"
rust-version = "1.95"

[dependencies]
tickglide-strategy = { path = "../" }

[dev-dependencies]
tickglide-strategy = { path = "../", features = ["test-util"] }
