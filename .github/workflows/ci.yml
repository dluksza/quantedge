name: CI

on:
  push:
    branches: [main]
  pull_request:

env:
  CARGO_TERM_COLOR: always
  RUSTFLAGS: -Dwarnings
  CARGO_INCREMENTAL: 0

jobs:
  fmt:
    name: Format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: Clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - run: cargo clippy --locked --workspace --all-targets --all-features

  test:
    name: Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@nextest
      - run: cargo nextest run --locked --workspace --all-features

  example-ta:
    name: tickglide-ta example
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/ta/example
      - run: cargo check --manifest-path crates/ta/example/Cargo.toml --locked

  example-strategy:
    name: tickglide-strategy example
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: crates/strategy/example
      - run: cargo build --manifest-path crates/strategy/example/Cargo.toml --locked --all-targets
      - run: cargo test --manifest-path crates/strategy/example/Cargo.toml --locked

  wasm:
    name: WASM
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - run: rustup target add wasm32-unknown-unknown wasm32-wasip1
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@wasmtime
      - run: cargo build --locked -p tickglide-core --lib --target wasm32-unknown-unknown
      - run: cargo test --locked -p tickglide-core --target wasm32-wasip1
      - run: cargo build --locked -p tickglide-ta --lib --target wasm32-unknown-unknown
      - run: cargo test --locked -p tickglide-ta --target wasm32-wasip1
      - run: cargo build --locked -p tickglide-strategy --lib --target wasm32-unknown-unknown
      - run: cargo test --locked -p tickglide-strategy --target wasm32-wasip1

  msrv:
    name: MSRV
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@1.95
      - uses: Swatinem/rust-cache@v2
      - run: cargo check --locked -p tickglide-core --lib --all-features
      - run: cargo check --locked -p tickglide-ta --lib --all-features
      - run: cargo check --locked -p tickglide-strategy --lib --all-features

  coverage:
    name: Coverage
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview
      - uses: Swatinem/rust-cache@v2
      - uses: taiki-e/install-action@cargo-llvm-cov
      - uses: taiki-e/install-action@nextest
      - run: cargo llvm-cov --locked nextest -p tickglide-core --all-features --lcov --output-path lcov-core.info
      - uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-core.info
          flags: tickglide-core
          fail_ci_if_error: false
      - run: cargo llvm-cov --locked nextest -p tickglide-ta --all-features --lcov --output-path lcov-ta.info
      - uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-ta.info
          flags: tickglide-ta
          fail_ci_if_error: false
      - run: cargo llvm-cov --locked nextest -p tickglide-strategy --all-features --lcov --output-path lcov-strategy.info
      - uses: codecov/codecov-action@v6
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: lcov-strategy.info
          flags: tickglide-strategy
          fail_ci_if_error: false
